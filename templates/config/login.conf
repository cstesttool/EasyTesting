# Sample config - config-driven test (single function, report at the end)
# Format:
#   headless=false  or  headed=true      - open browser in headed mode (visible window)
#   goto:<url>                          - open URL
#   <label>:<locator>=value:<text>       - type text into element (e.g. username:#user=value:john)
#   click=<locator>                      - click element (e.g. click=button[type="submit"])

# headed=true
goto:https://example.com
click=a
